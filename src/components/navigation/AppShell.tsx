"use client";import{useState}from"react";import{Trail}from"@/features/trail/Trail";import{Evolution}from"@/features/evolution/Evolution";import{Profile}from"@/features/profile/Profile";type View="journey"|"evolution"|"profile";const items:[View,string][]=[["journey","Jornada"],["evolution","Evolução"],["profile","Perfil"]];export function AppShell(){const[view,setView]=useState<View>("journey");return <div className="app-shell"><header className="topbar"><strong>CALIBRE</strong><span>SEU RITMO</span></header><main>{view==="journey"?<Trail/>:view==="evolution"?<Evolution/>:<Profile/>}</main><nav className="bottom-nav" aria-label="Navegação principal">{items.map(([id,label])=><button key={id} className={view===id?"active":""} aria-current={view===id?"page":undefined} onClick={()=>setView(id)}><i aria-hidden="true"/><span>{label}</span></button>)}</nav></div>}
