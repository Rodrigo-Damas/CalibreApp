"use client";
import { Search, X } from "lucide-react";import { useState } from "react";import { exercises } from "@/mocks/data";
export function ExerciseLibrary(){const [query,setQuery]=useState("");const filtered=exercises.filter(e=>(e.name+e.group).toLowerCase().includes(query.toLowerCase()));return <section className="page"><div className="page-title"><p className="eyebrow">BIBLIOTECA</p><h1>Exercícios</h1><p>Explore os movimentos do seu registro.</p></div><label className="search"><Search/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Buscar exercício ou grupo"/>{query&&<button onClick={()=>setQuery("")} aria-label="Limpar"><X/></button>}</label><div className="exercise-grid">{filtered.map(e=><article className="card exercise-card" key={e.id}><span>{e.icon}</span><div><h3>{e.name}</h3><p>{e.group}</p></div></article>)}</div>{!filtered.length&&<div className="empty card">Nenhum exercício encontrado.</div>}</section>}
