"use client";
import { ArrowRight, Check, Sparkles, X } from "lucide-react";import { useState } from "react";import { tracks } from "@/mocks/data";
const goals=["Ganhar força","Fazer minha primeira barra","Fazer mais flexões","Aprender movimentos avançados","Melhorar condicionamento","Treinar de forma geral"];
export function Onboarding({onClose}:{onClose:()=>void}){const [step,setStep]=useState(0);const [selected,setSelected]=useState<string[]>([]);return <div className="modal onboarding"><div className="onboarding-card"><button className="modal-close" onClick={onClose}><X/></button>{step===0?<><p className="eyebrow"><Sparkles/> PRIMEIRO ACESSO</p><h1>O que você quer conquistar?</h1><p>Escolha quantos objetivos quiser. Sua jornada não terá um nível único.</p><div className="goal-grid">{goals.map(g=><button className={selected.includes(g)?"selected":""} onClick={()=>setSelected(v=>v.includes(g)?v.filter(x=>x!==g):[...v,g])} key={g}>{selected.includes(g)&&<Check/>}{g}</button>)}</div><button className="primary-button" disabled={!selected.length} onClick={()=>setStep(1)}>CONTINUAR <ArrowRight/></button></>:<><p className="eyebrow">PONTO DE PARTIDA</p><h1>Uma trilha para cada capacidade.</h1><p>Estes testes fictícios criaram seu começo. Você poderá recalibrar sem reprovação.</p><div className="baseline-list">{tracks.map(t=><div key={t.id}><i style={{background:t.color}}>{t.icon}</i><span><strong>{t.name}</strong><small>{t.exercise}</small></span><b>{`${t.recommendation} ${t.unit}`}</b></div>)}</div><button className="primary-button" onClick={onClose}>ABRIR MINHA JORNADA <ArrowRight/></button></>}</div></div>}
