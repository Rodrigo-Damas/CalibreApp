import type{WorkoutSession}from"@/mocks/data";export function ConnectionLine({sessions}:{sessions:WorkoutSession[]}){return <svg className="connection-lines" aria-hidden="true" preserveAspectRatio="none">{sessions.filter(s=>s.prescriptions.length>1).map(s=><path key={s.id} d="M 12.5 50 C 12.5 20, 87.5 20, 87.5 50"/>)}</svg>}
