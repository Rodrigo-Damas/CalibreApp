import { fireEvent,render,screen } from "@testing-library/react";import { describe,it,expect } from "vitest";import { MockStoreProvider } from "@/mocks/store";import { Trail } from "./Trail";
describe("Trail",()=>{it("mostra cinco trilhas e monta uma sessão",()=>{render(<MockStoreProvider><Trail/></MockStoreProvider>);expect(screen.getByRole("button",{name:/Empurrar/})).toBeInTheDocument();expect(screen.getByRole("button",{name:/Cardio/})).toBeInTheDocument();fireEvent.click(screen.getAllByRole("button",{name:"ADICIONAR À SESSÃO"})[0]);expect(screen.getByText(/1 prática · ≈ 5 min/)).toBeInTheDocument()})});
