import {cleanup,fireEvent,render,screen,within} from "@testing-library/react";import {afterEach,describe,expect,it} from "vitest";import {Trail} from "./Trail";
afterEach(cleanup);
describe("Trail",()=>{it("ordena sessões independentes e omite challenges",()=>{render(<Trail/>);const row=document.querySelector('[data-date="2026-09-02"]')!;const nodes=within(row as HTMLElement).getAllByRole("button",{name:/Empurrar/});expect(nodes.map(n=>n.getAttribute("aria-label"))).toEqual([expect.stringContaining("06:50"),expect.stringContaining("12:20")]);expect(within(row as HTMLElement).getByRole("button",{name:"+1"})).toBeInTheDocument();expect(screen.queryByText(/challenge/i)).not.toBeInTheDocument()});it("conecta somente identificadores combinados",()=>{render(<Trail/>);expect(document.querySelectorAll('path[data-combined-id="combo-b"]')).toHaveLength(1);expect(document.querySelector('path[data-combined-id="s10"]')).toBeNull()});it("abre e fecha detalhes por botão, backdrop e Escape",()=>{render(<Trail/>);fireEvent.click(screen.getByRole("button",{name:/Empurrar, 07:10, 50/}));expect(screen.getByRole("dialog",{name:/Flexão/})).toHaveTextContent("Treino isolado");fireEvent.keyDown(document,{key:"Escape"});expect(screen.queryByRole("dialog")).not.toBeInTheDocument();fireEvent.click(screen.getByRole("button",{name:/Empurrar, 07:10, 50/}));fireEvent.click(screen.getByRole("button",{name:"Fechar detalhes"}));expect(screen.queryByRole("dialog")).not.toBeInTheDocument()})});
