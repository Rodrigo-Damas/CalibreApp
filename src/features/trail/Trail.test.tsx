import{cleanup,fireEvent,render,screen,within}from"@testing-library/react";import{afterEach,beforeEach,describe,expect,it,vi}from"vitest";import{createTimelineDates,localDateString,tracks}from"@/mocks/data";import{MockStoreProvider}from"@/mocks/store";import{Trail}from"./Trail";afterEach(cleanup);const scroll=vi.fn();beforeEach(()=>{localStorage.clear();Element.prototype.scrollIntoView=scroll;scroll.mockClear()});const setup=()=>render(<MockStoreProvider><Trail/></MockStoreProvider>);
describe("Trail",()=>{it("possui exatamente quatro trilhas",()=>expect(tracks).toHaveLength(4));it("termina hoje e não cria futuro",()=>{const d=createTimelineDates(),today=localDateString();expect(d.at(-1)).toBe(today);expect(d.every(x=>x<=today)).toBe(true)});it("abre posicionada em hoje e identifica cabeçalhos fixos",()=>{setup();expect(scroll).toHaveBeenCalledOnce();expect(screen.getByLabelText("Cabeçalhos fixos das trilhas")).toBeVisible();const dates=[...document.querySelectorAll<HTMLElement>("[data-date]")];expect(dates.at(-1)?.dataset.date).toBe(localDateString())});it("oferece Treinar somente hoje e abre montagem",()=>{setup();const row=document.querySelector<HTMLElement>(`[data-date="${localDateString()}"]`)!;expect(within(row).getAllByRole("cell")).toHaveLength(4);fireEvent.click(within(row).getByRole("button",{name:"Treinar"}));expect(screen.getByRole("dialog",{name:"Montar treino"})).toBeVisible()})});
