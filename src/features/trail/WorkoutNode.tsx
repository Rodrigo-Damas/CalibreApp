import type {CSSProperties} from "react";import {TrackIcon} from "@/components/icons/TrackIcon";import {tracks,type SessionEvent} from "@/mocks/data";
export function WorkoutNode({session,onSelect}:{session:SessionEvent;onSelect:(s:SessionEvent)=>void}){const track=tracks.find(t=>t.id===session.track)!;return <button className={`workout-node ${session.isRecord?"is-record":""}`} style={{"--track":track.color} as CSSProperties} onClick={()=>onSelect(session)} aria-label={`${track.name}, ${session.time}, ${session.volume} repetições${session.isRecord?", recorde":""}`}><TrackIcon track={session.track} decorative/><span>{session.time}</span><strong>{session.volume}</strong><small>rep.</small></button>}
