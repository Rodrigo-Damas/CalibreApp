import type {CSSProperties} from "react";import {TrackIcon} from "@/components/icons/TrackIcon";import {tracks,type TrackId} from "@/mocks/data";
export function TrackSelector({selected,onToggle}:{selected:TrackId[];onToggle:(id:TrackId)=>void}){return <div className="track-selector" aria-label="Escolha de trilhas">{tracks.map(track=><button key={track.id} style={{"--track":track.color} as CSSProperties} aria-pressed={selected.includes(track.id)} onClick={()=>onToggle(track.id)}><TrackIcon track={track.id}/><span>{track.name}</span></button>)}</div>}
