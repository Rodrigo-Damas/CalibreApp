import {act,cleanup,fireEvent,render,screen,within} from "@testing-library/react";
import {afterEach,describe,expect,it,vi} from "vitest";
import {MockStoreProvider} from "@/mocks/store";
import {Trail} from "@/features/trail/Trail";
afterEach(()=>{cleanup();vi.useRealTimers();localStorage.clear()});
function openTwo(){render(<MockStoreProvider><Trail/></MockStoreProvider>);fireEvent.click(screen.getByRole("button",{name:/Selecionar sugestão de Empurrar/}));fireEvent.click(screen.getByRole("tab",{name:"Puxar"}));fireEvent.click(screen.getByRole("button",{name:/Selecionar sugestão de Puxar/}));fireEvent.click(screen.getByRole("button",{name:"COMEÇAR"}))}
describe("WorkoutFlow",()=>{
 it("diferencia dose e repetições totais com a composição",()=>{openTwo();expect(screen.getByText("REPETIÇÕES TOTAIS")).toBeInTheDocument();expect(screen.getByText("105")).toBeInTheDocument();expect(screen.getAllByText("5 × 14 = 70 repetições").length).toBeGreaterThan(0);expect(screen.getAllByText("5 × 7 = 35 repetições").length).toBeGreaterThan(0);expect(screen.getAllByText("10 min").length).toBeGreaterThan(0)});
 it("inverte a ordem e persiste preferências de feedback",()=>{openTwo();fireEvent.click(screen.getByRole("button",{name:/Inverter ordem/i}));const cards=document.querySelectorAll(".practice-card");expect(within(cards[0] as HTMLElement).getByRole("heading",{name:"Barra fixa"})).toBeInTheDocument();fireEvent.click(screen.getByRole("button",{name:"Som"}));expect(localStorage.getItem("calibre-sound")).toBe("false")});
 it("avança automaticamente, pausa, retoma e encerra",()=>{vi.useFakeTimers();openTwo();fireEvent.click(screen.getByRole("button",{name:/INICIAR SESSÃO/}));expect(screen.getByText("AQUECIMENTO · 1/4")).toBeInTheDocument();for(let i=0;i<5;i++)act(()=>vi.advanceTimersByTime(1000));expect(screen.getByText("AQUECIMENTO · 2/4")).toBeInTheDocument();fireEvent.click(screen.getByRole("button",{name:"Pausar treino"}));act(()=>vi.advanceTimersByTime(10000));expect(screen.getByText("AQUECIMENTO · 2/4")).toBeInTheDocument();fireEvent.click(screen.getByRole("button",{name:"Continuar treino"}));for(let i=0;i<15;i++)act(()=>vi.advanceTimersByTime(1000));expect(screen.getByText(/SÉRIE 1\/5/)).toBeInTheDocument();fireEvent.click(screen.getByRole("button",{name:"Encerrar treino"}));expect(screen.queryByRole("dialog",{name:"Sua sessão"})).not.toBeInTheDocument()});
});
