import{cleanup,fireEvent,render,screen}from"@testing-library/react";import{afterEach,beforeEach,describe,expect,it}from"vitest";import{MockStoreProvider}from"@/mocks/store";import{WorkoutFlow}from"./WorkoutFlow";afterEach(cleanup);beforeEach(()=>localStorage.clear());const setup=()=>render(<MockStoreProvider><WorkoutFlow onClose={()=>{}}/></MockStoreProvider>);function select(...names:string[]){names.forEach(n=>fireEvent.click(screen.getByRole("button",{name:new RegExp(n)})));fireEvent.click(screen.getByRole("button",{name:"Continuar"}));}
describe("WorkoutFlow",()=>{it("seleciona de uma a quatro trilhas e pergunta condição depois",()=>{setup();expect(screen.getAllByRole("button",{pressed:false})).toHaveLength(4);select("Empurrar","Puxar","Pernas","Core");expect(screen.getByText("Como você chega para este treino?")).toBeVisible()});it("não mostra formato para uma trilha e mostra para duas",()=>{setup();select("Empurrar");fireEvent.click(screen.getByRole("radio",{name:/Normal/}));fireEvent.click(screen.getByRole("button",{name:/Curto/}));expect(screen.queryByText("Como as trilhas se alternam?")).not.toBeInTheDocument();cleanup();setup();select("Empurrar","Puxar");fireEvent.click(screen.getByRole("radio",{name:/Normal/}));fireEvent.click(screen.getByRole("button",{name:/Longo/}));expect(screen.getByText("Como as trilhas se alternam?")).toBeVisible()});it("mantém controles independentes e prescrições distintas",()=>{setup();select("Empurrar","Puxar");fireEvent.click(screen.getByRole("radio",{name:/Normal/}));fireEvent.click(screen.getByRole("button",{name:/Curto/}));fireEvent.click(screen.getByRole("button",{name:/Circuito/}));expect(screen.getByLabelText("Aumentar repetições de Empurrar")).toBeVisible();expect(screen.getByLabelText("Diminuir repetições de Puxar")).toBeVisible();expect(screen.getByText(/14 por série · 5 minutos/)).toBeVisible();expect(screen.getByText(/7 por série · 5 minutos/)).toBeVisible()})});
