import{describe,expect,it}from"vitest";import{conventionalLimit,estimatedVolume,longReps,recommend,sequence}from"./recommendationEngine";import type{WorkoutSession}from"@/mocks/data";
const session=(reps:number,perception:WorkoutSession["perception"],status:WorkoutSession["status"]="completed"):WorkoutSession=>({id:crypto.randomUUID(),combinedId:"x",date:"2026-01-01",time:"10:00",duration:"short",format:"blocks",arrival:"normal",plannedMinutes:5,elapsedSeconds:300,status,perception,prescriptions:[{track:"push",exerciseKey:"floor-push-up",suggestedShortReps:7,chosenShortReps:reps,displayedReps:reps,sets:5,estimatedVolume:reps*5}]});
describe("recommendationEngine",()=>{it("converte curto em longo arredondando para cima",()=>{expect(longReps(7)).toBe(4);expect(longReps(14)).toBe(7);expect(estimatedVolume(7,"long")).toBe(40)});it("usa arredondamento convencional no limite",()=>{expect(conventionalLimit(1)).toBe(1);expect(conventionalLimit(14)).toBe(17)});it("avança após duas percepções na medida",()=>{expect(recommend("push",7,[session(7,"right"),session(7,"right")]).value).toBe(8)});it("alivia após dois limites e nunca abaixo de um",()=>{expect(recommend("push",1,[session(1,"limit"),session(1,"limit")]).value).toBe(1)});it("ignora interrupções sem quebrar sequência",()=>{expect(recommend("push",7,[session(7,"right"),session(7,undefined,"interrupted"),session(7,"right")]).value).toBe(8)});it("ordena blocos e circuito preservando doses",()=>{const x=[{track:"push"as const},{track:"pull"as const}];expect(sequence(x,2,"blocks").map(i=>i.track)).toEqual(["push","push","pull","pull"]);expect(sequence(x,2,"circuit").map(i=>i.track)).toEqual(["push","pull","push","pull"])})});
