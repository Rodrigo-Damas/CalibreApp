export type SignalKind="prepare"|"start"|"finish";export type SignalPreferences={sound:boolean;volume:number;vibration:boolean};
let context:AudioContext|null=null;async function audioContext(){const C=window.AudioContext??(window as typeof window&{webkitAudioContext?:typeof AudioContext}).webkitAudioContext;if(!C)return null;context??=new C();if(context.state==="suspended")await context.resume();return context}
export async function enableSignalAudio(){return audioContext()}
export async function playSignal(kind:SignalKind,p:SignalPreferences){if(p.vibration&&"vibrate"in navigator)navigator.vibrate(kind==="prepare"?18:kind==="start"?45:[60,40,90]);if(!p.sound)return;const a=await audioContext();if(!a)return;const freqs=kind==="prepare"?[880]:kind==="start"?[392,523]:[523,659,784];freqs.forEach((f,i)=>{const o=a.createOscillator(),g=a.createGain(),at=a.currentTime+.005+i*.025;o.frequency.value=f;g.gain.setValueAtTime(.0001,at);g.gain.exponentialRampToValueAtTime(Math.max(.001,p.volume*.16),at+.012);g.gain.exponentialRampToValueAtTime(.0001,at+.2);o.connect(g).connect(a.destination);o.start(at);o.stop(at+.22)})}
